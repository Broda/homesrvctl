# Node Template Placeholder

`{{ hostname }}` was initialized with the `node` placeholder template.

Replace the placeholder service in `docker-compose.yml` with your real Node application container when ready.
